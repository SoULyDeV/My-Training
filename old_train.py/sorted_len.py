def sort_by_length(arr):
    return sorted(arr, key=len)
        



print(sort_by_length(["Telescopes", "Glasses", "Eyes", "Monocles"]))
print(sort_by_length(["", "strings", "motocycle", "paralelepipede"]))
